[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "raw-renderer"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy", "Pillow"]

[tool.setuptools.packages.find]
include = ["raw_renderer*"]
