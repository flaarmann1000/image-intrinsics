.vscode

# ── build / env ──────────────────────────────────────────────────────────────
__pycache__
/pycache/*
.claude
.env
.env*.local
venv/
.venv/
*.egg-info/

# ── vendored / downloaded ────────────────────────────────────────────────────
1.10.0.tar.gz
cub-1.10.0/

# ── generated output ─────────────────────────────────────────────────────────
# Everything now lives under results/ (see idr/paths.py); the pre-consolidation
# entries for synthetic_ct/, mit_results/, render/, ... are gone with them.
results/
wandb/
data/

# Source data: the 3D-Front scans are large and fetched separately.
datasets/3D-Front/

# ── tests ────────────────────────────────────────────────────────────────────
# The golden .npz baseline and noise.json ARE committed; scene/ is regenerated by
# tests/golden.py build_scene().
tests/golden/scene/

# ── web app scaffolding ──────────────────────────────────────────────────────
.next/
out/
node_modules/
.vercel/
*.tsbuildinfo

local_datasets/